model User {
  id          String   @id @default(uuid())
  email       String   @unique
  name        String?
  avatar      String?
  provider    String?   
  providerId  String?
  password    String?
  createdAt   DateTime @default(now())
}